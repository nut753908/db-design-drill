name: db-design-drill

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: dbdesigndrill
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
